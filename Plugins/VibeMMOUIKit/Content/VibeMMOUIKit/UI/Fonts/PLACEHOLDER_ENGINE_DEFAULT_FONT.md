# Placeholder Font

The placeholder font for this plugin is Unreal Engine's built-in UI font resolved in C++ through `FCoreStyle`.

No font binary is stored in this folder. Import your own licensed font and assign it in `UVibeMMOUIStyleDataAsset`.
