; RED HUD has its own 3840x2160 ScaleBox-based canonical canvas.
; Do not apply additional per-widget RenderTransform scaling.
; Keep the project DPI rule enabled; the root ScaleBox compensates by fitting the canonical canvas
; to the logical viewport while preserving the 16:9 aspect ratio.
