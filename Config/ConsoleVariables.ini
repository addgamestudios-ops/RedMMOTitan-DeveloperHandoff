[Startup]
; RTX 3080 10 GB / 32 GB host-RAM guardrails.  The July 14 failures were host
; commit exhaustion while asset compilation was allowed to run without a hard
; memory budget.  Keep editor background work bounded so the desktop remains
; responsive while the 50 km planet is being authored or cooked.
Editor.AsyncAssetCompilationMaxMemoryUsage=12
Editor.AsyncAssetCompilationMaxConcurrency=2
